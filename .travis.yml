language: node_js
node_js:
- '8'

#cache:
#  directories:
#    - node_modules


script:
  - .travis/build.sh
  
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN  # Set in travis-ci.org dashboard, marked secure
  local-dir: compiled/build
  on:
    branch: master
